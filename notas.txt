Posts y likes

-Debemos completar el reto de colocar un id en la ruta y que nos devuleva ula info. de 
  personas que siguen al usuario de ese id.
-Para ello, se harán algunas cosas distintas.

network:
  -Usaremos el get, la ruta será '/:id/following'.
  -El id que colocaremos en el metodo lo sacaremos de la ruta.
  -El metodo del controller a usar será following, que crearemos.

controller:
  -Traemos el id por parametro.
  -Declaramos un objeto 'join' vacío por ahora.
  -Al elemento 'user' de ese objeto, le damos como valor 'user_to'.
  -Creamos otro objeto 'query', cuyo elemento user_from tenga como valor el id.
  -Retornamos el metodo query, la tabla será user_follow (tabla creada anteriormente), y 
    damos el query, y pasamos tambien el objeto join.

mysql:
  -Modificamos la función query para que reciba el join como 3er parametro.
  -Creamos la variable 'joinQuery' con un string vacío.
  -Sí pasamos el 3er parametro, le damos como valor a este el sigiente query:
    .JOIN [key] ON [table].[val] = [key].id
    .key: user.
    .table: tabla de follow_user.
    .val: user_to
  -Retornaremos la promesa, pero el query quedará así:
    .SELECT * FROM [tabla] [joinQuery] WHERE [table].?
    .El ? quedaría igual.

-Ahora vamos a crear la entidad 'post' para almacenar los post de cada usuario.
-La tabla tendra:
  .id: del post
  .text: de tipo TEXT, sin un tamaño definido. Para agregar el contenido del post.
  .user: Id del usuario que escribió el post.
-Por ahora, usaremos solo el get, pero el reto es crear todos los demás metodos.