Aislar el código de la base de datos

-Crearemos en la raíz la carpeta store, donde guardaremos las conexiones 
  a todas las DB.
-Creamos un archivo dummy donde simularemos metodos aplicados a las DB.
-Como las DB tendrán tablas, los metodos serán distintos.
-Las conexiones si serán las mismas, donde el controller traiga al dummy, 
  y el network al controller.