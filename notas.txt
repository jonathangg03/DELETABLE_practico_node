Estructura inicial del proyecto: API y rutas

-Por ahora, tendremos varios archivos y carpetas:
  .En la raíz, tendremos las carpetas, y el archivo de config. donde
    configuramos las variables que usaremos en multiples archivos.
    Tendremos la carpeta network con las respuestas,
  .En la carpeta 'api' tendremos todo, el index será el equivalente a 
    server en el curso pasado, y los componentes.
